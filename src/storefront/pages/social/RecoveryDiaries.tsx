import { MaesPlaceholder } from '../../components/maes/MaesPlaceholder';

export default function RecoveryDiaries() {
    return (
        <MaesPlaceholder
            title="Historias de Recuperación"
            type="Article"
            objective="Conexión emocional y confianza"
            tags={["Technical", "Storytelling", "Social Proof"]}
        />
    );
}
